These tracks were generated by various AI models using this prompt:

```
Using the documentation in @docs/manual.md, write a ZFM MML track. The theme/style/genre/etc. of the track is up to you.

You may use predefined patches from the patch banks under @patches/ to avoid having to make your own from scratch.

Reference the source code of the synth, compiler, etc. only if the documentation is unclear.

To check that your track is valid, run `cli/zig-out/bin/zfm -o rendered.wav your-track.zfm`.
```

But the methodology wasn't very good, because each model was able to see the previous models' tracks in the tracks directory.
